apiVersion: v1
kind: Service
metadata:
  name: bootstrap
  labels:
    app: bootstrap
spec:
  clusterIP: None
  selector:
    app: bootstrap
  ports:
  - port: 80
    protocol: TCP
    name: http
    targetPort: http
  - port: 443
    protocol: TCP
    name: https
    targetPort: https
