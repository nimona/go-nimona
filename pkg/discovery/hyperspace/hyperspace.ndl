package nimona.io/discovery/hyperspace

import nimona.io/crypto crypto

domain ContentProvider {
    signed command Requested {
        bloomFilter repeated int
    }
    signed event Updated {
        bloomFilter repeated int
    }
}
